export const metadata = { title: 'iThing — IoT Solutions', description: 'Purpose-built IoT platforms for hospitality, buildings, industrial, agriculture, logistics, and healthcare.' };
export default function RootLayout({ children }) {
  return children;
}
